package DAO;

public interface IPersonne {
    // une interface est une classe ou on definit les signatures de methode
    // une interface est une classe qui n'a pas d'attribut
    // on peut l'eriter ou l'implementer
    public void saisie();
    public void affiche();
}
